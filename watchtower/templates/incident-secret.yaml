apiVersion: v1
kind: Secret
metadata:
    name: incident-secret
    # prettier-ignore
    namespace: {{ .Values.namespace }}
type: Opaque
stringData:
    DATABASE_URL: "postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:5432/incident_service_db"
    JWT_ACCESS_SECRET: "{{ .Values.auth.config.jwtAccessSecret }}"
