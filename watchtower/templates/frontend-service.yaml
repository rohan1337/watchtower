apiVersion: v1
kind: Service
metadata:
    name: frontend
    # prettier-ignore
    namespace: {{ .Values.namespace }}
spec:
    type: ClusterIP
    selector:
        app: frontend
    ports:
        - port: 80
          # prettier-ignore
          targetPort: {{ .Values.frontend.port }}
