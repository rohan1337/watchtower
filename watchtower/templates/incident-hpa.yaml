apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
    name: incident-service-hpa
    # prettier-ignore
    namespace: {{ .Values.namespace }}
spec:
    scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: incident-service

    # prettier-ignore
    minReplicas: {{ .Values.incident.autoscaling.minReplicas }}
    # prettier-ignore
    maxReplicas: {{ .Values.incident.autoscaling.maxReplicas }}

    metrics:
        - type: Resource
          resource:
              name: cpu
              target:
                  type: Utilization
                  # prettier-ignore
                  averageUtilization: {{ .Values.incident.autoscaling.targetCPUUtilization }}
